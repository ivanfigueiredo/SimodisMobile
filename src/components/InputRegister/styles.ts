import { StyleSheet } from "react-native";

export const styles = StyleSheet.create ({
    container:{
        backgroundColor: 'white', 
        borderRadius: 10,
        width: '80%',
        height: 30,
        marginTop: 8,
        marginBottom: 5,
        padding: 20
    },
})